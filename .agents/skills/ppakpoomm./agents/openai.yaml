interface:
  display_name: "Ppakpoomm."
  short_description: "Repo-specific patterns and workflows for ppakpoomm."
  default_prompt: "Use the ppakpoomm. repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true